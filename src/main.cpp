#include "Utilities.h"

int main() {
//    CNode<string> cNode("+");
//    CNode<string> cNode1("da");
//    CNode<string> cNode2("niel");
//    vector<CNode<string> > children;
//    children.push_back(cNode1);
//    children.push_back(cNode2);
//    cNode.setChildren(children);
//
//    vector<string> variables;
//
//
//    cout << cNode.compute(variables, variables);
    Utilities utilities;
    utilities.initialize();
}
